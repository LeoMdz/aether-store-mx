import { themes } from "../themes";
export function Hero() {
  const order = [themes.freefire, themes.fortnite, themes.xbox];
  return `<section class="hero" id="inicio" aria-labelledby="hero-title"><div class="hero-worlds" aria-hidden="true">${order.map((t) => `<div class="hero-world" data-game="${t.id}" data-theme="${t.id}"><img data-src="${t.image}" alt="" width="1440" height="960"><div class="world-tint"></div><span class="world-label">${t.wordmark}</span>${t.id === "freefire" ? '<span class="crystal crystal-one"></span><span class="crystal crystal-two"></span>' : t.id === "fortnite" ? '<span class="vcoin coin-one">V</span><span class="vcoin coin-two">V</span>' : '<span class="circuit"></span>'}</div>`).join("")}</div>
 <div class="hero-content container" data-reveal><div class="hero-caption"><span class="small-line"></span> TRES UNIVERSOS. UN SOLO LUGAR. <span class="small-line"></span></div><h1 id="hero-title">¡POTENCIA<br class="mobile-break"> TU JUEGO!</h1><p class="hero-subtitle">TUS RECARGAS <span>AL INSTANTE.</span></p><p class="hero-description">Diamantes, Game Pass y regalos. Tú eliges cómo jugar.</p><div class="hero-ctas">${order.map((t) => `<a class="game-button" data-theme="${t.id}" data-game="${t.id}" href="#precios" data-select="${t.id}"><i data-lucide="${t.icon}"></i>${t.name}<i data-lucide="arrow-up-right"></i></a>`).join("")}</div><div class="hero-note"><i data-lucide="zap"></i> Sin vueltas. Directo a tu juego.</div></div><div class="hero-bottom"><span>AETHER STORE / DIGITAL GOODS</span><a href="#catalogo">DESCUBRE TU SIGUIENTE NIVEL <i data-lucide="arrow-down"></i></a><span>MXN · LATAM</span></div></section>`;
}
