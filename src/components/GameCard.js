export function GameCard(theme, index) {
  return `<article class="game-card" data-theme="${theme.id}" data-game="${theme.id}" data-reveal><a href="#precios" data-select="${theme.id}" class="card-art" aria-label="Ver catálogo de ${theme.name}"><img data-src="${theme.image}" alt="Arte del catálogo ${theme.name}" width="1440" height="960"><div class="art-shade"></div><span class="card-category">${theme.unit}</span><span class="game-wordmark">${theme.wordmark}</span><span class="card-arrow"><i data-lucide="arrow-up-right"></i></span></a><div class="card-copy"><div class="card-meta"><span>${theme.delivery}</span><span>0${index + 1}</span></div><h3>${theme.title}</h3><p>${theme.description}</p><div class="card-bottom"><div><span class="from">DESDE</span><span class="card-price">$${theme.priceFrom} <small>MXN</small></span></div><a class="card-cta" href="#precios" data-select="${theme.id}">${theme.action}<i data-lucide="arrow-right"></i></a></div></div></article>`;
}
